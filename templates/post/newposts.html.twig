{% extends 'base.html.twig' %}

{% block body %}
    <h1>Criar Post</h1>
    
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
    <a href="{{ path('post') }}">Back</a>

{% endblock %}